# Stage 6.5 optional NVIDIA runtime for Windows x64 / Python 3.14.
# The local version pins force the CUDA 13.0 wheels from the official
# PyTorch package index while the remaining dependencies come from PyPI.
# Install after requirements.lock with:
# python -m pip install -r requirements-diarization-cuda.lock
--extra-index-url https://download.pytorch.org/whl/cu130

pyannote.audio==4.0.7
torch==2.11.0+cu130
torchaudio==2.11.0+cu130
torchcodec==0.13.0+cu130
